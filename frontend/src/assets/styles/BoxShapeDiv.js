import styled from 'styled-components';

const BoxShapeDiv = styled.div`
    background: #fff;
    border: 1px solid #ccc;
    border-top: none;
    border-color: rgba(0,0,0,.2);
    box-shadow: 0px 0px 5px rgba(0,0,0,.2);
    color:#000;
    border-radius: 8px;
`

export default BoxShapeDiv;