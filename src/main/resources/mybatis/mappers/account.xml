<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="account">

	<select id="findByEmailAndPassword" parameterType="map" resultType="account">
		<![CDATA[
			SELECT 
				no, name
			FROM 
				account 
			WHERE email = #{email} AND password = #{password}
		]]>
	</select>

	<insert id="insert" parameterType="account">
		<![CDATA[
			insert 
				into account 
				values(null,#{email},#{password},#{nickname},#{phoneNumber},#{name},#{thumbnailUrl}, now());
		]]>
	</insert>

	<select id="isExistEmail" parameterType="string" resultType="String">
		<![CDATA[
			select 
				email
			from 
				account
			where 
				email = #{email}
		]]>
	</select>

	<select id="isExistNickName" parameterType="string" resultType="String">
		<![CDATA[
			select 
				nickName
			from 
				account
			where 
				nickName = #{nickName}
		]]>
	</select>

	<select id="isExistPhoneNumber" parameterType="string" resultType="String">
		<![CDATA[
			select 
				phone_number
			from 
				account
			where 
				phone_number = #{phone_number}
		]]>
	</select>



	<update id="update" parameterType="account">
		<choose>
			<when test='password == ""'>
				<![CDATA[
				update account
			       set name=#{name },
			           gender=#{gender }
			     where id=${id }
				]]>
			</when>
			<otherwise>
				<![CDATA[
				update account
				   set name=#{name },
				       password=#{password }, 
				       gender=#{gender }
				 where id=${id }	
				]]>
			</otherwise>
		</choose>
	</update>

	<update id="updateNickname" parameterType="account">
		<![CDATA[
		    update account
		    set nickname=#{nickname }
		    where no=${no }
		]]>
	</update>

    <update id="updatePassword" parameterType="account">
		<![CDATA[
		    update account
		    set password=#{password}
		    where no=${no }
		]]>
	</update>

    <update id="updateUserInfo" parameterType="account">
		<![CDATA[
		    update account
		    set name=#{name },
		        phone_number=#{phoneNumber}
		    where id=${id }
		]]>
	</update>

	<select id="searchAccount" parameterType="map"
		resultType="account">
		<![CDATA[
<<<<<<< HEAD
			select email, password
				from account 
				where name = #{name} 
				and email = #{email}
		]]>
	</select>

	<select id="searchEmail" parameterType="map"
		resultType="account">
		<![CDATA[
			select email
				from account 
				where name = #{name} 
				and phone_number = #{phoneNumber}
		]]>
	</select>

	<update id="updateRendomPassword" parameterType="map">
		<![CDATA[
			update account
			    set password=#{randomPassword }
			    where email=#{email }
		]]>
	</update>

=======
		select no, email, nickname
		  from account 
		 where email = #{email } 
		   and password = #{password } 
		   ]]>
	</select>

	<update id="updateToken" parameterType="account">
		<![CDATA[
				update account 
				set token= #{token}
				where no = #{no}
		  ]]>
	</update>

	<select id="findByNo" parameterType="account"
		resultType="account">
		<![CDATA[
		select no, email, nickname, phone_number, thumbnail_url
		  from account 
		 where no = #{no } 
		   ]]>
	</select>
>>>>>>> origin/sewon
</mapper>